server:
  compression:
     enabled: true
     mime-types: application/json,application/xml,text/html,text/xml,text/plain
  server-header: hub-servicenow-connector
  error:
    include-message: always


management:
  endpoints:
    web.base-path: /